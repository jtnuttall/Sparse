import { CombatLogConstants } from './combat-log-constants.model';

describe('CombatLogConstants', () => {
  it('should create an instance', () => {
    expect(new CombatLogConstants()).toBeTruthy();
  });
});
